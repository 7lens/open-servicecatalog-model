# Golden-example findings register

**Date:** 2026-09-13  
**Scope:** `examples/reference-enterprise/golden-example/` plus
cross-checks against source, full `reference-enterprise`,
`reference-estate`, basic `examples/`, and public docs.  
**Schema:** not changed. **Examples:** not changed this pass.

Severity: BLOCKER = teaches a fundamentally wrong OSM concept.
HIGH = a sophisticated copier could build a bad catalog.
MEDIUM = meaningful ambiguity. LOW = clarity only.

Do not treat naming taste as a finding.

| ID | Severity | Location | Finding | Evidence | Recommended Action |
|---|---|---|---|---|---|
| G-001 | HIGH | `golden-example/catalog/services.yaml` `sec.iam` offerings | Offerings are ITSM request types, not technological variants. | Source `sec.iam` offerings are directory-lifecycle, password-reset, MFA, access-governance. Golden kept directory-lifecycle and mfa. MODEL.md: an Offering is a **variant** (environment, provider, operating model, packaging). Switching Entra→Okta would not use these offering ids; requesting “MFA” vs “directory” is a request catalog, not a provider/environment variant. | After review: either one identity Offering (e.g. entra) with request types outside OSM, or document explicitly that this enterprise’s predecessor used request-catalog offerings and OSM adopters should not copy that grain. |
| G-002 | HIGH | `golden-example/catalog/ict-providers.yaml` `certifications[]`; golden README | Provider program names will be read as “this estate’s services are certified.” | AWS/Microsoft/Red Hat/Oracle lists are copied from source. Golden YAML/README never say they are unscoped program names. Parent enterprise README says this; Phase 9 is golden-folder-only. | State the scope rule in the golden README next to the provider table. Do not add a schema field. |
| G-003 | HIGH | `golden-example/posture/service-posture.yaml` ISO/NIST/GDPR/AI Act rows | Framework mapping fields are presented as imported operational truth. | Predecessor `service_attributes.yaml` header: representative subset **chosen to demonstrate every field pattern**, with comments “EXAMPLE 2 — PII”, “EXAMPLE 3 — restricted”, “EXAMPLE 5 — AI”. Golden copies `iso27001_controls`, `iso27701_pii_role: processor`, `gdpr_processing_activity: true`, `nist_control_status: implemented`, `ai_act_risk_class: high-risk` as if assessed. SPECIFICATION: framework-named fields are optional mappings, not certification. | Label these posture rows as predecessor demo values, or omit all but canonical rto/rpo/chargeback until a real assessment exists. |
| G-004 | HIGH | `examples/catalog/` vs `golden-example/` | Two contradictory “correct” VM/Kubernetes/provider patterns ship in the same repo. | Basic: `providers: [aws, azure]` on the VM **Service**; offerings `shared`/`dedicated` (tenancy); ICT Provider id `azure`; Kubernetes offerings `compute.kubernetes.aws` / `.azure`. Enterprise golden: providers on Offerings; offerings by cloud; provider id `microsoft`; Kubernetes offerings `aws-eks` / `azure-aks` / `openshift`. MODEL.md still shows `compute.kubernetes.aws`. | After review: align basic examples with the legal-seller + capability+offering pattern, or stamp basic examples as “schema tutorial only, do not copy modelling.” |
| G-005 | HIGH | `compute.kubernetes` and `db.managed-relational` service posture | Service-level posture leaked from one product after collapse. | Source `vendor_support_status: active` and `last_verified: 2026-03-15` are EKS-only; `2026-02-01` is RDS Oracle-only. Golden puts them on the capability Service while correctly refusing `operational_criticality`. Same leakage class they already identified. | Keep service-level posture unset on collapsed Services; keep offering posture only on the sourced offering. |
| G-006 | HIGH | `auto.configuration-automation` `providers: [redhat]` on the Service | Capability Service bound to one seller as if intrinsic. | MODEL: service-level `providers` when the provider is **intrinsic**. Configuration automation is not intrinsically Red Hat. Only the Ansible offering should list `redhat`. Copying this pattern makes every one-vendor estate freeze the vendor into the Service. | Remove service-level `providers` here (and similarly review `sec.iam`). Keep offering-level `providers`. |
| G-007 | MEDIUM | `db.managed-relational.rds-oracle` `providers: [aws, oracle]` | Dual provider may encode an indirect engine relationship. | Source `dora_third_party_deps: [aws, oracle]`. AWS operates RDS; Oracle is the engine brand/licensor. Contrast: Snowflake+AWS is control-plane + host (external context). Copying this teaches “list every brand on the offering.” | Keep as source-faithful but document: second id is engine licensor, not a second operator — or drop `oracle` if OSM `providers` means who delivers the offering. Decision candidate D-GE-002. |
| G-008 | MEDIUM | `compute.kubernetes.openshift` | OpenShift collapsed into Kubernetes may over-unify a platform. | Source Service `compute.openshift` with offerings `openshift-platform` **and** `container-platform` (duplicative, not EKS-like). Golden dropped the second offering and placed OpenShift beside EKS/AKS. External context: OpenShift is a Kubernetes distribution plus platform services. Source treated it as its own Service. | Either keep the collapse and say “this estate’s OpenShift is consumed as managed Kubernetes”, or keep OpenShift as its own Service. Do not leave the tension implicit. |
| G-009 | MEDIUM | `storage.backup.backup`, `sec.secrets.secrets-mgmt`, `auto.aiops.aiops` | Singleton Offerings with no variant dimension. | OSM requires `minItems: 1` offering. Source also had tautological ids (`sec.secrets.secrets-mgmt`). Backup source *did* have a variant (`restore`) that golden dropped, leaving a worse singleton. | Prefer keeping a real variant (backup vs restore) or document “schema requires one offering; this is not a modelling variant.” |
| G-010 | MEDIUM | Characteristic `deployment_environment` | Duplicates offering identity and collides with “environment.” | `compute.virtual-machines.aws` has `deployment_environment: aws`. Basic examples use characteristic `environment: production`. Allowed values include `gcp` with no GCP VM offering. OpenShift has no `deployment_environment` while EKS/AKS do. | Use `providers[]` as the cloud dimension; reserve `environment` for delivery stage if used at all; do not list unused allowed values. |
| G-011 | MEDIUM | Service `lifecycle_state: production` (all except `auto.aiops`) | Lifecycle invented for services without source attributes. | Only six source health records had `lifecycle_state`. `auto.aiops` is `pilot` from source (good). Others assumed production because they appeared in the live catalog. CONTRIBUTING: do not fill customer fields from imagination. | Leave a note in golden README, or only include services whose lifecycle is sourced. |
| G-012 | MEDIUM | Provenance `source_record_id` / `last_verified` on collapsed Services | Provenance names one ancestor and a date that is a security-review date. | `compute.kubernetes` provenance `source_record_id: compute.eks`, `last_verified: 2026-03-15` (EKS `last_security_review`). AKS and OpenShift disappear from the trail. `2026-01-01` on other records is not in source. | Multi-source collapse should not pretend to be a single imported record. Use a cataloguing date or omit `last_verified`. |
| G-013 | MEDIUM | `compute.functions` / `auto.configuration-automation` / `auto.aiops` | Single-product capability Services. | Source had only Lambda, only Ansible, only AIOps. Capability names are right (Q3: adding Azure Functions would not change `compute.functions`). Copiers will still see “Lambda Service” because there is one product offering and no sibling. | Keep the capability id; add a one-line “one offering today ≠ product-Service” note in the golden README. |
| G-014 | MEDIUM | Golden README vs posture YAML | Isolation: seven services have no posture; README does not say so. | Counts table: 3 posture records. Walk-through only explains Kubernetes unknown criticality. Copier may infer posture is always rich when present, or always required. | State: most golden services have **no** posture because the predecessor had no health record for them. |
| G-015 | MEDIUM | `microsoft.type: cloud-infrastructure` | Merge discarded source type split. | Source `azure` = cloud-infrastructure, source `microsoft` = cloud-platform (identity/productivity). Golden uses cloud-infrastructure for Entra + Azure VMs. OSM `type` cannot be both. | Document coarseness; do not invent `microsoft-azure` as a second legal entity. Decision D-GE-005. |
| G-016 | MEDIUM | Cross-example identity | Workforce identity modelled three different ways. | Basic examples: no identity service. Estate golden: `identity.directory-idp` vs `identity.cloud-authorization`. Enterprise golden: `sec.iam` with AD/Entra request offerings. Source had no AWS IAM — not inventing it is correct. Copiers will not know which pattern is OSM. | Point estate vs enterprise at the same identity rule: cloud IAM ≠ directory; this enterprise only had directory in source. |
| G-017 | MEDIUM | Cross-example Kubernetes offerings | Same Service, different offering grain. | MODEL: `compute.kubernetes.aws`. Basic: `.aws`. Estate: `.aws-eks` + `.self-operated`. Enterprise: `.aws-eks` + `.openshift`, no self-operated. Source had no self-operated Kubernetes Service. | Document that offering slugs are estate-specific; MODEL.md example should not look like a second canonical. |
| G-018 | LOW | `os_family` configurable on the Service, not the Offering | Works, but easy to miss. | Source split Linux/Windows as Services, cloud as offerings. Combining is correct. Characteristic sits on the Service so all clouds share it — matches source (both OS on aws/azure/on-prem). | Optional: repeat `os_family` on offerings only if values differ. No schema change. |
| G-019 | LOW | `version: "1.0.0"` / `valid_from: 2026-01-01` | OSM-required metadata, not source operational dates. | OSM Service requires version and valid_from. Source spec is “Version 1.0”. Fine if labelled cataloguing metadata. | One sentence in golden README. |
| G-020 | LOW | Full `reference-enterprise` vs golden | Expected subset differences look like errors if both are “the example.” | Full has `lz.landing-zone`, `net.*`, `mw.*`, `gcp`, Confluent, GitHub; golden does not. Full IAM has `access-governance`; golden does not. | README already says golden is a subset. Keep. |
| G-021 | LOW | `service_attributes` string in golden README | Vocabulary lesson vs obsolete term sitting in the golden folder. | Line 68 cites predecessor `service_attributes`. Necessary for traceability; a skimmer may think it is current OSM. | Keep the word only as “predecessor field, now `offering_posture`.” |

No BLOCKER: the example does not teach “EKS is a Service.” That was the predecessor’s fundamental error and it was reversed.

No mechanical fixes were required for validation.
